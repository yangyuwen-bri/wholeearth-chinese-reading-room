# Leaf 025 Review

## Conclusion

accepted

## Reasons

- The Audel list contains all 56 entries in the printed two-column reading
  order. Titles, AUD identifiers, volume/set qualifiers, and visible prices
  agree with the page. `AUD-6` genuinely has no printed price and is correctly
  marked “原刊未列” rather than inferred.
- The list's postpaid condition and shared order block are complete:
  Theo. Audel & Company, 4300 West 62nd Street, Indianapolis, Indiana `46206`.
- The three sample items remain correctly attributed to `Carpenters and
  Builders Guide`. The gambrel-roof account retains the `2×8` tie, `2×6`
  rafters and braces, knee brace and studs, `24"` framing centers, `5'10"` and
  `14'0"` vertical dimensions, and five-ply `2×12` girder. The post-foundation
  and screwdriver explanations are faithful, including the `3½'` depth,
  rock/concrete base, plumb-bob leveling, tamping, tapered sides, parallel
  sides, and downward-pressure explanation.
- The independent `Glue-nailed Roof Trusses` record is not merged into the
  Audel list. It retains 14 truss plans at `50¢` each, `15¢` circulars, and the
  Small Homes Council-Building Research Council / University of Illinois /
  Urbana, Illinois address.
- The nailing and assembly instructions are accurate: `4d` nails for gussets,
  `6d` nails for scabs and splice plates, two rows at `4"` spacing, buried nail
  heads, glue marking, flipping the truss, the other-side pieces, and a
  `24-hour` curing period before handling.
- The bracing plan correctly retains trusses at `2' o.c.`, approximately
  `45°` diagonal bracing, and the continuous quarter-span bracing
  relationships.
- The `2/12` material quantities are complete: two top chords, two bottom
  chords, two long diagonals, two short diagonals, two large heel gussets, two
  small heel gussets, two intermediate gussets, one peak gusset, two splice
  plates, two scabs, glue, and nails. The cutting-schedule summary correctly
  retains the `20'–28'` span range, row subjects, pattern-truss instruction,
  overhang rule, and reuse of short-diagonal offcuts.
- Regression review confirms that the gambrel-roof account now distinguishes
  the single `2"×12"` joist from the paired `2–2"×12"` joist and retains both
  `10'0"` half-spans.
- The cutting-schedule scope is now explicit: `20'–28'`, with `0"`, `4"`, and
  `8"` subdivisions for each whole-foot span, hence four-inch increments. The
  uncertainty notes and self-critique have been updated consistently.

## Required Fixes

- None.

## Residual Risks

- The cutting schedule contains many small per-cell dimensions. Summarizing
  its verified range, subdivisions, row meanings, and usage rules without
  inventing an exhaustive cell transcription is appropriate.
- A few tiny construction-panel labels remain degraded in the source
  reproduction. Unresolved labels should remain marked uncertain rather than
  be normalized from modern truss practice.
