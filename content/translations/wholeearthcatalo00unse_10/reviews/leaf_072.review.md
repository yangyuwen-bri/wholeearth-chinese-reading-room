# Leaf 072 Review

## Conclusion

accepted

## Reasons

- The revised plot line now preserves all 12 source nodes in order. `Key
  inserted into lock` and `Door handle turns` are separate nodes; the victim
  is `asleep in bed`; and the late node retains both `Victim falls` and `Door
  shuts, cutting off light`.
- The contributory-factor table restores the `Dialogue` row and describes its
  progression from the intruder's first speech through the exchange before the
  shot. The camera-control sequence includes `DEPRESSED SHOT`, and the editing
  row retains three distinct `CUT` markers in plot-line order rather than
  reducing them to a generic editing summary.
- The crowd-simulation section now retains the legible `GLASS SHEET` label and
  correctly attaches it to the foreground-figure method.
- Both lighting diagrams retain their numeric `0°` nodes. The horizontal
  diagram correctly distinguishes `0°` direct frontal lighting from `180°`
  direct back light; the vertical diagram separately identifies its `0°`
  level/viewpoint position and the directly centered back-light position as
  `INEFFECTUAL (UNLESS OFF CENTRE)`.
- All four abstract sound strings are preserved as source-language examples:
  `whooooooooooooooooooooooooooo`, `erchrrerr crehrrrrrrer`,
  `crehrrrr erchrrrrererer`, and `woowoowoowoowoowoowoowoowoo`. In particular,
  the second example begins `erch`, matching the 300-dpi scan.
- No regression was found in the previously complete surrounding material:
  the two book entries remain distinct, `给画面切换员（vision mixer）的指令`
  now identifies a person consistently with the table's operational commands,
  the switcher table and crowd caption remain intact, and the lower
  `Sound and Vision` procedures and script example retain their prior coverage.

## Required Fixes

- None.

## Residual Risks

- The plot chart depends on horizontal alignment, and the sound strings use
  long runs of similar letters. The 300-dpi page rendering and source pack
  resolve the reviewed labels, but later reformatting should preserve the
  current order and exact source-language strings.
